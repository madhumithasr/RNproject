import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    icon: {
        width: 30,
        height: 30,

    },
    title: {
        fontSize: 12,
        color: '#000',
        marginHorizontal: 8,

    }
});

export default styles;